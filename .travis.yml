sudo: false

language: java

jdk:
  - oraclejdk8

cache:
  directories:
    - $HOME/.m2

os:
  - linux

env:
  matrix:
    -
    - MAVEN_JACOCO=true
    - MAVEN_RETROLAMBDA=true
    - MAVEN_JACOCO=true MAVEN_RETROLAMBDA=true
